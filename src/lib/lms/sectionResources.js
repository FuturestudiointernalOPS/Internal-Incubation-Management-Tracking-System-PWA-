/**
 * Facade — kept during the MVC migration so importers of
 * "@/lib/lms/sectionResources" keep resolving unchanged.
 * New code should import from "@/models/lms/sectionResources".
 */
export * from "@/models/lms/sectionResources";
